[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "anti-chimera"
version = "0.1.0"
description = "Reuse-first anti-chimera training scaffold"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["anti_chimera*"]
